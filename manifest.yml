---
applications:
- name: ((app-name))
  buildpacks:
      - ruby_buildpack
  memory: 64M
  instances: ((instances))
  routes:
      - route: ((route-external))    
  stack: cflinuxfs4
  command: rails server